import * as React from 'react';
import { Button } from '@mui/material';
import Box from '@mui/material/Box';

const SuccessScreen = (props) => {
    return (
        <Button>dsffsd</Button> 
    );
}
export default SuccessScreen;